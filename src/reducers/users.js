const usersReducer = (state = [], action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default usersReducer;





// export default (state = [], action) => {
//   switch (action.type) {
//     default:
//       return state
//   }
// }